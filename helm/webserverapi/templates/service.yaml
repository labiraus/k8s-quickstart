apiVersion: v1
kind: Service
metadata:
  name: {{ include "webserverapi.fullname" . }}
  labels:
    {{- include "webserverapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webserverapi.selectorLabels" . | nindent 4 }}
